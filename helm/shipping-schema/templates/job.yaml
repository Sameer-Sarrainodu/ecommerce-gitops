apiVersion: batch/v1
kind: Job

metadata:
  name: shipping-schema

  annotations:
    argocd.argoproj.io/sync-wave: "1"

spec:

  backoffLimit: 1

  ttlSecondsAfterFinished: 300

  activeDeadlineSeconds: 600

  template:

    spec:

      restartPolicy: Never

      containers:

      - name: shipping-schema

        image: sameerroboshopacr.azurecr.io/shipping-schema:v1

        env:

        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: root-password

        command:
        - sh
        - -c
        - |
          set -e

          echo "Waiting for MySQL..."

          until nc -z mysql 3306
          do
            sleep 5
          done

          echo "Loading schema.sql"

          mysql \
            -h mysql \
            -uroot \
            -p${MYSQL_ROOT_PASSWORD} \
            < /schema.sql

          echo "Loading app-user.sql"

          mysql \
            -h mysql \
            -uroot \
            -p${MYSQL_ROOT_PASSWORD} \
            < /app-user.sql

          echo "Loading master-data.sql"

          mysql \
            -h mysql \
            -uroot \
            -p${MYSQL_ROOT_PASSWORD} \
            cities \
            < /master-data.sql

          echo "Shipping schema completed successfully"